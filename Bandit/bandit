use lib '/Users/liqin/.cpan/build/';
use Net::SSH::Perl;

sub bandit_ {
    my($user, $host, $port, $pass)=@_;
	my $ssh = Net::SSH::Perl->new($host, port=>$port);
	$ssh->login($user, $pass);
	$ssh;
}

1;